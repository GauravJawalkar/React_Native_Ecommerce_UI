export interface topWeekData {
    id: number,
    image: any,
    title: string,
    price: number
}

export interface ProductCardProps {
    id: number,
    title: string,
    imageLink: any
}

export interface CategoryItem {
    _id: number;
    category: string;
    imageLink: any;
}